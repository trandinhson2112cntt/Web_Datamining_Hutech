@model IEnumerable<Web_Datamining.Models.KhoaHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Khóa Học</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NamHoc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NamHoc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaKhoaHoc }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaKhoaHoc }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaKhoaHoc })
        </td>
    </tr>
}

</table>
